name: artifact-stage
description: Session-driven shared artifact pane — open/scroll/zoom PDFs, images, text and HTML in the desktop app from any session; pane reports display receipts.
version: 0.1.0
